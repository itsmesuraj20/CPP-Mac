#include <bits/stdc++.h>
using namespace std;

int main() {
    int a = 1;
    int b = 0;
   

    

    // Perform XOR for all combinations and print
    cout << "a ^ a = " << (a ^ a) << endl;
    cout << "a ^ b = " << (a ^ b) << endl;
    // cout << "a ^ c = " << (a ^ c) << endl;
    // cout << "a ^ d = " << (a ^ d) << endl;

    cout << "b ^ a = " << (b ^ a) << endl;
    cout << "b ^ b = " << (b ^ b) << endl;
    // cout << "b ^ c = " << (b ^ c) << endl;
    // cout << "b ^ d = " << (b ^ d) << endl;

    // cout << "c ^ a = " << (c ^ a) << endl;
    // cout << "c ^ b = " << (c ^ b) << endl;
    // cout << "c ^ c = " << (c ^ c) << endl;
    // cout << "c ^ d = " << (c ^ d) << endl;

    // cout << "d ^ a = " << (d ^ a) << endl;
    // cout << "d ^ b = " << (d ^ b) << endl;
    // cout << "d ^ c = " << (d ^ c) << endl;
    // cout << "d ^ d = " << (d ^ d) << endl;

    return 0;
}